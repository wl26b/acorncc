node_modules/
dist/

# Assembly we emit while compiling.
*.s

# The test suite is a separate upstream repo; don't vendor it into ours.
writing-a-c-compiler-tests/

# Machine-local tool state, not project config — the permission allowlist and
# transcripts are this machine's, not the repo's.
.claude/

# macOS.
.DS_Store
